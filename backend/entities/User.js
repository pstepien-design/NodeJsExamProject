class User {
    constructor(email, firstName, lastName, id) {
        this.email = email;
        this.firstName = firstName;
        this.lastName = lastName;
        this.id = id;
    }
}

export default User;