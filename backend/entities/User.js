class User {
  constructor(email, firstName, lastName, id, hasClicked) {
    this.email = email;
    this.firstName = firstName;
    this.lastName = lastName;
    this.id = id;
    this.hasClicked = hasClicked;
  }
}

export default User;
